# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/internal/step-group.cue
# Create-if-absent (oam.definition.createIfAbsent; or adopt if this release already owns it) so vela-core and
# vela-workflow can both ship overlapping WorkflowStepDefinitions without Helm
# ownership conflicts on dual install.
{{- $defNs := include "systemDefinitionNamespace" . -}}
{{- $defName := "step-group" -}}
{{- $existingDef := lookup "core.oam.dev/v1beta1" "WorkflowStepDefinition" $defNs $defName -}}
{{- if eq (include "oam.helm.manageIfAbsentOrOwned" (list $existingDef .)) "true" }}
apiVersion: core.oam.dev/v1beta1
kind: WorkflowStepDefinition
metadata:
  annotations:
    custom.definition.oam.dev/category: Process Control
    definition.oam.dev/description: A special step that you can declare 'subSteps' in it, 'subSteps' is an array containing any step type whose valid parameters do not include the `step-group` step type itself. The sub steps were executed in parallel.
  name: step-group
  namespace: {{ include "systemDefinitionNamespace" . }}
spec:
  schematic:
    cue:
      template: |
        // no parameters, the nop only to make the template not empty or it's invalid
        nop: {}
{{- end }}
